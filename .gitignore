__pycache__/
*.py[cod]
.Python
.venv/
venv/
.env
*.egg-info/
.pytest_cache/

# 별도 Git 저장소 (GitHub: catena_X_project) — 상위 repo에 포함하지 않음
catena_X_project/

# Catena-X mock store (edc.py onboard 결과)
/store/
/catena-x/store/

# 로컬 mock 실행 산출물 (onboard 로 재생성 가능)
/data/
/catena-x/data/

# 텔레메트리 서버(server/app.py) 수신 JSON
/catena-x/server/data/
